name: freestyle
on:
  workflow_dispatch:
jobs:
  build:
    runs-on:
      - windows-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: run batch command
      shell: cmd
      run: dir
